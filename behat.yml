
default:
  extensions:
    Laracasts\Behat: ~
#      env_path: "%root_path%/.env.behatt"
    Behat\MinkExtension:
      default_session: laravel
      laravel: ~
  suites:
    default:
      contexts:
        - ExampleContext
        - WelcomeContext
        - LoginContext
