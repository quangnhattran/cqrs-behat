default:
  # extensions:
  # Laracasts\Behat:
  #     env_path: .env.behat
  # Behat\MinkExtension:
  #     default_session: laravel
  #     base_url: http://localhost:8000
  #     laravel: ~
  suites:
    default:
      contexts:
        - WelcomeContext
    phase1:
      contexts:
        - LoginContext
#            table: users
        - PostContext
